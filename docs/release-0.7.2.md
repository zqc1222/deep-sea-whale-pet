# 深海鲸灵 0.7.2

## 本次更新：打赏安全加固

在「打赏作者」区块加入两道防线，防止山寨版偷换收款码：

- **防山寨提示**：明确提醒用户只从官方 GitHub Release 下载，打赏前核对校验码，谨防山寨版偷换收款码。
- **正版校验码**：设置页「打赏作者」区块显示本机安装包的正版校验码（`resources/app.asar` 的 SHA-256 前 12 位）。
  - 与官方 Release 页公布的正版校验码**一致** → 官方版本，可放心打赏。
  - **不一致** → 疑似被改过的版本，请立即停止使用并重新从官方下载。

本版本正版校验码：

```
2D37A9219133
```

## 说明

- 校验码只对安装版生效；浏览器演示 / 开发版不显示。
- 代码未签名时，校验码是用户核验"这是不是官方原版"的可靠手段；正规的代码签名证书仍是最终方案。
- 防山寨提示与校验码不改变任何打赏流程，只是多一道信任核对。

## 已验证

- TypeScript 严格类型检查与 Electron 生产构建通过。
- 校验码由构建产物 `win-unpacked/resources/app.asar` 实时计算，与 Release 附件中的安装包内容一致。

## 安装说明

- 安装版：运行 `DeepSeaWhalePet-Setup-0.7.2-x64.exe`，可选择安装目录。
- 便携版：直接运行 `DeepSeaWhalePet-Portable-0.7.2-x64.exe`。
- 当前没有商业代码签名证书，Windows SmartScreen 可能显示"未知发布者"。

这是原创非官方同人概念原型，与 DeepSeek 官方无隶属、授权或背书关系。
